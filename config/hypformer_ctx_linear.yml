_include_yml: hypformer_ctx_full

detection:
  graph_preprocessing:
        intra_graph_batching:
          used_methods: none
  gnn_training:
    encoder:
      hypformer:
        # OOM-safe variant for larger windows/batches.
        attention_type: linear_focused
